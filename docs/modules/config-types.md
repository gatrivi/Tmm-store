# Config and types

Purpose: plan matrix and shared TypeScript models.

Paths:

- `src/config/plans.ts` — Plan enum, `PlanFeatures`, gate helpers
- `src/types/order.ts` — OrderRecord, OrderStatus
- `src/types/promotion.ts` — Promotion, `calculateDiscount`
- `src/types/tenant.ts` — TenantRecord
- `src/data/menu.ts` — MenuItemType seed data

See also: [features/plans-tiers.md](../features/plans-tiers.md)
